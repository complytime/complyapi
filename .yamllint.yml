---
extends: default

rules:
  comments: disable
  document-start: disable
  empty-lines:
    level: warning
  indentation:
    spaces: consistent
  line-length: disable
  truthy:
    allowed-values: ["true", "false", "on"]
